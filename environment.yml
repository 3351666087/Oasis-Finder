name: mesh_supply_chain
channels:
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
      - fastapi
      - uvicorn[standard]
      - python-multipart
      - SQLAlchemy
      - PyMySQL
      - pandas
      - numpy
      - Faker
      - matplotlib
      - networkx
      - scikit-learn
      - xgboost
      - ortools
      - python-dotenv
      - python-docx
      - pillow
